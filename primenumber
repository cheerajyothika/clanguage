#include<stdio.h>
int main()
{
int a;
printf("enter a number");
scanf("%d", &a);
int count =0;
for(int i=2;i<a;i++)
{
    if(a%i==0)
    {
        count++;
    }
}
if (count==0)
{
    printf("it is a prime");
}
else
{
    printf("it is not a prime");
}
}
